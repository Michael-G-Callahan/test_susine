% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_results.R
\name{build_confusion_matrix}
\alias{build_confusion_matrix}
\title{Build pre-aggregated confusion matrix from SNPs dataset.}
\usage{
build_confusion_matrix(
  snps_dataset_path,
  snp_files = NULL,
  run_table,
  grouping_vars,
  pip_bucket_width = 0.01,
  use_cases_path = NULL,
  output_path,
  stream = FALSE,
  confusion_bins_path = NULL,
  confusion_bins_files = NULL
)
}
\arguments{
\item{snps_dataset_path}{Path to the aggregated snps_dataset folder (partitioned parquet). Optional when \code{snp_files} supplied.}

\item{snp_files}{Optional character vector of parquet file paths (e.g., staging flush outputs) to stream over.}

\item{run_table}{Data frame with run metadata (must contain run_id and grouping columns).}

\item{grouping_vars}{Character vector of column names to group by.}

\item{pip_bucket_width}{Numeric bucket width for PIP binning (default 0.01).}

\item{use_cases_path}{Optional path to use_cases.csv for label lookup.}

\item{output_path}{Path where the CSV will be written.}

\item{stream}{When TRUE, aggregates per file and combines results (recommended when using staging \code{snp_files}).}
}
\value{
The confusion matrix tibble (invisibly).
}
\description{
Creates a compact summary table with per-bucket causal/non-causal counts
at each PIP threshold, grouped by the specified variables. Stores per-bucket
counts (NOT cumulative) to allow flexible downstream aggregation.
}
