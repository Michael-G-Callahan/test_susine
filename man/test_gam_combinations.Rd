% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_metrics.R, R/x_matrix_metrics.R
\name{test_gam_combinations}
\alias{test_gam_combinations}
\title{Test all 1-way and 2-way GAM models for X-metrics vs AUPRC}
\usage{
test_gam_combinations(
  df_joined,
  metric_names = NULL,
  auprc_var = "AUPRC",
  out_path = NULL,
  k_smooth = 5,
  cross_axis_only = FALSE
)

test_gam_combinations(
  df_joined,
  metric_names = NULL,
  auprc_var = "AUPRC",
  out_path = NULL,
  k_smooth = 5,
  cross_axis_only = FALSE
)
}
\arguments{
\item{df_joined}{Data frame with X metrics and AUPRC columns}

\item{metric_names}{Character vector of metric columns to test}

\item{auprc_var}{AUPRC column name (can be raw, residualized, or ranked)}

\item{out_path}{Optional path to save CSV results}

\item{k_smooth}{Basis dimension for smooth terms (default 5)}

\item{cross_axis_only}{If TRUE, only test 2-way models where one metric is
from Axis A (basin size) and one from Axis B (identifiability). This is
much more efficient and theoretically motivated than all pairs.}
}
\value{
Tibble with model diagnostics for all combinations

Tibble with model diagnostics for all combinations
}
\description{
Systematically fits GAMs with all single metrics and all pairs of metrics
to identify which metrics (and interactions) best predict AUPRC.

Systematically fits GAMs with all single metrics and all pairs of metrics
to identify which metrics (and interactions) best predict AUPRC.
}
